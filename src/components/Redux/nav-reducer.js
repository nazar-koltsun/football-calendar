let initState = {
    isNavOpen: false,
};

const navReducer = (state = initState, action) => {
    // switch (action.type) {
    //     case SET_USER_DATA:
    //         return {
    //             ...state,
    //             ...action.data,
    //             isAuth: true,
    //         };
    //     default:
    //         return state;
    // }

    return state;
};

export default navReducer;